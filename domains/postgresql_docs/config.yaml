name: postgresql_docs
documents_path: ./domains/postgresql_docs/data
chunk_size: 900
chunk_overlap: 150
retrieval_k: 6
evaluation_rules:
  - not_grounded_check
  - overconfident_language_check
domain_prompt_prefix: |
  You are answering questions about the official PostgreSQL documentation.
  Prefer authoritative explanations grounded in the docs.
  If you are not sure, say you don't know.
allowed_file_types:
  - sgml
max_docs: 600
